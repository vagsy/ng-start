import { SeventhDirective } from './seventh.directive';

describe('SeventhDirective', () => {
  it('should create an instance', () => {
    const directive = new SeventhDirective();
    expect(directive).toBeTruthy();
  });
});
