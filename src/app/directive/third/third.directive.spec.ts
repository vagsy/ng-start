import { ThirdDirective } from './third.directive';

describe('ThirdDirective', () => {
  it('should create an instance', () => {
    const directive = new ThirdDirective();
    expect(directive).toBeTruthy();
  });
});
