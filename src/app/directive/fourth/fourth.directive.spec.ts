import { FourthDirective } from './fourth.directive';

describe('FourthDirective', () => {
  it('should create an instance', () => {
    const directive = new FourthDirective();
    expect(directive).toBeTruthy();
  });
});
